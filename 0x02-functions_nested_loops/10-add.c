#include "main.h"

/**
 * add - a functioon that adds two integers
 * and this returns a result.
 *
 * @a: number one.
 * @b: number two
 *
 * Return: Add of number one and number two.
 */
int add(int a, int b)
{
	return (a + b);
}
